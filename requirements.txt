# Direct runtime dependencies, version-pinned (==) so two official builds embed
# byte-identical code. Transitive deps + hashes are expanded by CI into
# requirements.lock (pip-compile --generate-hashes); see that file's header.
#
# Target runtime: CPython 3.13 (x64) — the newest Python with stable cp313 wheels
# for numpy and the CPU-only onnxruntime. Python 3.14 would force a numpy dev
# build and the onnxruntime-directml variant (ships DirectML.dll), so the engine
# stays on 3.13 until stable cp314 wheels exist.
google-genai==2.17.0
sounddevice==0.5.5
# numpy 2.5.x and scipy 1.18.x dropped Python 3.11 (Requires-Python >=3.12),
# breaking the CI test matrix's 3.11 leg — hold both back until 3.11 support
# is dropped from quality.yml's matrix or these get a 3.11-compatible patch.
numpy==2.5.2
onnxruntime==1.28.0
soxr==1.1.0
scipy==1.17.1
python-dotenv==1.2.2
# Migration of legacy Fernet-wrapped BYOK slots (migrate-on-read decoder in
# byok_store). DPAPI is the at-rest wrapper now; this only decrypts old slots.
cryptography==50.0.0
pywebview==6.2.1
pycaw==20251023
# keyboard installs a low-level global hook (admin/AV-sensitive, unmaintained).
# Since app/global_hotkey.py (ctypes RegisterHotKey), this is no longer on the
# always-on path — _register_hotkeys() uses that instead, so the hook is not
# installed for the app's whole lifetime. Still needed for webui.py's
# capture_hotkey()/cancel_hotkey() (Settings' "press a key" recorder), which
# has to detect an arbitrary combo the user is currently pressing — something
# RegisterHotKey can't do — but that only runs for the few seconds the
# recording box is open, not for the whole session.
keyboard==0.13.5
PyAudioWPatch==0.2.12.8
# Local neural TTS for the cascade free tier (app/local_tts.py). Piper VITS
# voices run on the bundled onnxruntime; voice models download at runtime.
sherpa-onnx==1.13.4
